import styled from '@emotion/styled';
import { Box, Card, CardContent, CardMedia, Grid, Typography,Button} from '@mui/material';


export const Image = styled.img`
 
  width: 100px;
  height: 70px;

`;
export const AvdCardMedia = styled(CardMedia)({
  width: '200px',
});